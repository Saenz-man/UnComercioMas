# .env.production
NODE_ENV=production
PORT=3000

# Estas credenciales son las mismas que usa el túnel SSH,
# pero como está en el servidor, no se necesita el túnel.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=uncomerciomas_user
DB_PASSWORD=tu_llave_secreta_super_larga_y_unica
DB_NAME=uncomerciomas_db

# --- CONFIGURACIÓN DE SEGURIDAD (JWT) ---
JWT_SECRET=tu_llave_secreta_super_larga_y_unica
JWT_EXPIRATION_TIME=1h